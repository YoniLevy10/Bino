'use client'

import { BamakorSystemMap3D } from '../components/system-map/BamakorSystemMap3D'

/**
 * מפת שלד 3D של ארכיטקטורת Bamakor — לשימוש פנימי / תיעוד ויזואלי.
 * נגיש ב־/system-map (לא מופיע בתפריט הצד).
 */
export default function SystemMapPage() {
  return <BamakorSystemMap3D />
}
